package org.jboss.deltacloud.client;

import java.net.URL;
import java.util.List;

import org.jboss.deltacloud.client.model.Flavor;
import org.jboss.deltacloud.client.model.Image;
import org.jboss.deltacloud.client.model.Instance;
import org.jboss.deltacloud.client.model.Realm;

public class RESTClient 
{
	public static void main(String[] args) throws Exception
	{
		DeltaCloudClient api = new DeltaCloudClient(new URL("http://localhost:3003/api"), "mockuser", "mockpassword");
		//Instance instance = api.listInstances("inst3");
		//api.rebootInstance("inst3");
		
		//Realm realm = api.listRealms("us");
		
		//System.out.println(realm.getId());
		
		//Instance i = api.listInstances("inst3");
		for(Instance i : api.listInstances())
		{
			System.out.println(i.getId() + " " + " " + i.getName() + " " + i.getOwnerId() + " " + i.getPublicAddresses().get(0));
		}
		
	}
}
