# DeltaCloud Java Client Library

1. Build Library
2. Dependencies
3. Examples can be found in examples/../Driver.java


1. Build Library

DeltaCloud Client Lib requires Maven to build, see http://maven.apache.org
for instructions on how to set this up.

Once Maven is installed, go to the directory where you checked out the project and type "mvn package"

This should generate the deltacloud-client.jar in the target directory.

2. Dependencies

DeltaCloud Java Client depends on the following: 
 - Apache HTTPComonents
 - log4j

